name: Hello World CI

on:
  push:
    branches:
      - main
      - spyne/ci
  pull_request:
    branches:
      - main

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Hello World
        run: echo "Hello, World!"
